1. What is the falsely explained about conditional statements
and repetitive statements?
- 2. The variable that can be used in a switch statement are
int, double.
2. Convert the left switch statement into an Expression then
write on the right.
<br>String grade = "B";
<br><br>int score1 = 0;
<br>switch (grade) {
<br>case "A":
<br>score1 = 100;
<br>break;
<br>case "B":
<br>int result = 100 - 20;
<br>score1 = result;
<br>break;
<br>default:
<br>score1 = 60;
<br>}
<br><br>...
<br><br>String grade = "B";
<br><br>int score = switch(grade) {
<br>case "A" -> 100;
<br>case "B" -> {
<br>int result = 100 - 20;
<br>yield result;
<br>}
<br>default -> 60;
<br>};
3. Using for statement, write a code that prints all the
sum of multiples of 3 from integer range 1 to 100.
<br>int result1 = 0;
<br>for(int i = 1; i<100; i++) {
<br>if(i % 3 == 0) {
<br>result1 += i;
<br>}
<br>}
<br>System.out.println(result1);
