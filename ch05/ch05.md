1. What is the wrong description about the reference types
(참조 타입)?
- 2. A reference type variable's memory generation location
is Stack(스택).
2. What is not a member that consists in a class?
- 4. Local varible(로컬 변수)
3. What is the wrong description about the String type?
- 2. String type's string comparison has to use ==.
4. What is the wrong way of declaring an Array(배열)?
- 2. int[] array; array = {1, 2, 3};
5. What is wrong about the default initial value(초기값) of an
Array?
- 3. Default initial value of boolean type Array item
(배열 항목) is 'true'.
6. The following code is to show length of an array. Write the
execution result.
<br>int[][] array = {
<br>{95, 86},
<br>{83, 92, 96},
<br>{78, 83, 93, 87, 88}
<br>};
<br>
<br>System.out.println(array.length);
<br>System.out.println(array[2].length);
<br>- 3
<br>- 5
<br>7. From the given array item, write a code that prints the
maximum value(use a for statement).
<br>int[] array = {1, 5, 3, 8, 2};
<br>
<br>int max = -999999;
<br>for(int i = 0; i < array.length; i++) {
<br>if (array[i] > max) {
<br>max = array[i];
<br>}
<br>}
<br>System.out.println(max);
<br>8. Write a code that gets the sum and average of the given
array item(use a nested for statement).
<br>int[][] array = {
<br>{95, 86},
<br>{83, 92, 96},
<br>{78, 83, 93, 87, 88}
<br>};
<br>
<br>int sum1 = 0; int cnt1 = 0;
<br>for(int i = 0; i < array.length; i++) {
<br>for(int j = 0; j < array[i].length; j++) {
<br>sum1 = sum1 + array[i];
<br>cnt1++;
<br>}
<br>}
<br>System.out.println(sum1);
<br>System.out.println((double) sum1 / cnt1);
